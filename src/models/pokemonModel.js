export class Pokemon {
    constructor(id, name, img, types, stats) {
      this.id = id;
      this.name = name;
      this.img = img;
      this.types = types;
      this.stats = stats;
    }
  }
  